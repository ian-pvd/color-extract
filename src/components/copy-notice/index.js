/**
 * Copy Notice
 *
 * Markup for copy event notice.
 */

import './style.scss';

const CopyNotice = () => (
    <div className="copy-notice">
        <span>Copied <tt></tt> to clipboard.</span>
    </div>
);

export default CopyNotice;
