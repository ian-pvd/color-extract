/**
 * Header
 *
 * Site header markup.
 */

import './style.scss';

const Header = () => (
	<header className="header">
		<h1 className="header__title">Color Extract</h1>
	</header>
);

export default Header;
