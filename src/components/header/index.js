/**
 * Header
 *
 * Site header markup.
 */

import './style.scss';

const Header = () => (
	<header className="header">
		<h1 className="header__title">Color Extract</h1>
    <p className="header__description">A web-based javascript applet that extracts color values from (a block of mixed text).</p>
	</header>
);

export default Header;
